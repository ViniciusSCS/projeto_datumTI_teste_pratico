<form method="post" action="{{ path('hash_generates_delete', {'id': hash_generate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hash_generate.id) }}">
    <button class="btn">Delete</button>
</form>
